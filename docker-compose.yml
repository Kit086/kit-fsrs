services:
  fsrs-cards:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - fsrs-data:/data
    environment:
      - APP_DATA_DIR=/data
      - SESSION_SECRET=change-this-to-a-random-secret
    restart: unless-stopped

volumes:
  fsrs-data:
